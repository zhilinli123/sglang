name: Publish llm-multimodal

permissions:
  contents: read

on:
  push:
    branches: [main]
    paths: [multimodal/Cargo.toml]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/publish-crate
        with:
          crate: llm-multimodal
          path: multimodal
          token: ${{ secrets.CARGO_REGISTRY_TOKEN }}
