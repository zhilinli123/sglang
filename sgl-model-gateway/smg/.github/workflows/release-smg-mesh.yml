name: Publish smg-mesh

permissions:
  contents: read

on:
  push:
    branches: [main]
    paths: [mesh/Cargo.toml]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/publish-crate
        with:
          crate: smg-mesh
          path: mesh
          token: ${{ secrets.CARGO_REGISTRY_TOKEN }}
